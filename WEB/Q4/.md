Level  >> MID
Category >> Web

---FOR PLAYERS---
-----------------NAME >> Divine Access II
-----------------DESCRIPTION >> The Lord's Chamber awaits.
                                 The registry holds many souls.
                                 But only one sits on the throne.
                                 Find the king. Bend the seal.
                                 Enter the sanctum.
                                 Use this seal to enter: bruce / brucenighthunter
-----------------LINK >> https://divine-access-2.vercel.app/


---FOR ADMINS---

-----------------HINT >> The registry answers anyone who asks — if you know what to ask for.
                          Identities are small numbers in disguise. The seal trusts whatever it's told.
-----------------HOW TO SOLVE >>
--------------------------------1. Login with bruce / brucenighthunter (POST /api/login, sets divine_token cookie)
--------------------------------2. Redirected to /chamber → /api/me confirms rank PAWN
--------------------------------3. Profile link reveals /user/37693cfc748049e45d87b8c7d8b9aacd
                                    (your own hash, fetched via /api/profile?hash=...)
--------------------------------4. View page source / Network tab → no user DB exposed anywhere in JS anymore;
                                    everything routes through /api/* server endpoints
--------------------------------5. Notice /api/profile?hash=<hash> returns ANY user's username/role/hash
                                    as long as you're logged in — no ownership check (IDOR)
--------------------------------6. Realize registry IDs are small sequential integers — script a brute force:
                                    for id in range(-100, 51): hash = md5(str(id))
                                                                 GET /api/profile?hash=<hash>
                                    Look for the response with "role": "admin"
--------------------------------7. Hit → belor (hash: ac8044b0a0ee6c0ff51fa4ed4df1010e) → role: admin
--------------------------------8. Visit /admin as bruce → /api/admin returns 403, denied (you're still "user")
--------------------------------9. Open DevTools → Application → Cookies → grab divine_token
--------------------------------10. Decode the middle segment (base64url) → JWT-style payload,
                                     spot "alg": "HS256", "role": "user"
--------------------------------11. Forge a new token:
                                     - header: {"alg":"none","typ":"JWT"} (base64url encode)
                                     - payload: change role → "admin", optionally username/hash → belor's
                                       (base64url encode)
                                     - signature: leave empty, keep the trailing dot
                                     - result: header.payload.
--------------------------------12. Set the forged cookie and reload:
                                     document.cookie = "divine_token=<forged_token>; Path=/";
                                     window.location.href = "/admin";
--------------------------------13. /api/admin re-parses the cookie server-side, accepts alg:none, returns the vault path
--------------------------------14. Admin panel reveals → /sys/core/admin/x7k2/secret/realm/vault
--------------------------------15. Navigate to vault → /api/vault confirms admin role server-side → flag revealed

-----------------FLAG >> CyC0g{y0u_4r3_7h3_d1v1n3_l0rd_n0w}

-------------(Made By sc0d3r)--------------
