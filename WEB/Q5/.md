Level  >> MID
Category >> Web

---FOR PLAYERS---
-----------------NAME >> Micro-Slop
-----------------DESCRIPTION >> You Just Hacked into a system but u can't use anything its using window
                                 defender to defend it lets see if u can bypass it
-----------------LINK >> https://micro-slop.vercel.app/

---FOR ADMINS---

-----------------HINT >> The terminal only accepts the next step in order — order matters as much as content.
                          Four techniques, four commands, one flag. The file itself refuses you directly;
                          it only listens once it's been convinced you're already SYSTEM.
-----------------HOW TO SOLVE >>
--------------------------------1. Open Terminal
--------------------------------2. Run: mklink /j link target        → "Junction created."
--------------------------------3. Run: echo payload > file.txt:ads  → "Payload written."
--------------------------------4. Run: MpCmdRun.exe -Scan            → "Scan initiated. Race condition triggered."
--------------------------------5. Run: wer queue report              → "SYSTEM privileges obtained via RoguePlanet."
--------------------------------6. Run: whoami                        → "nt authority\system"
--------------------------------7. Run: type flag.txt                 → redirected to /api/flag → flag returned
--------------------------------NOTE >> Steps must be run in this exact order. Backend tracks progress via
                                          signed tokens, so skipping/reordering/guessing the flag URL fails.

-----------------FLAG >> CyC0g{r0gu3_pl4n3t_2025_w1n_0_d4y}

-------------(Made By sc0d3r)--------------