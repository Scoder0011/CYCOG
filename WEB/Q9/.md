Level  >> HARD
Category >> Web
---FOR PLAYERS---
-----------------NAME >> Kaboom
-----------------DESCRIPTION >> The server accepts .zip files. That's all it says it does.
                                 
-----------------LINK >> https://kaboom-u6bx.onrender.com
---FOR ADMINS---
-----------------HINT >> Optional (default: none) — "3 doors, 10 rooms each, then something waiting behind the last one"
-----------------HOW TO SOLVE >>
--------------------------------1. Upload any normal .zip — server responds "rejected", vague reason, no exact spec given
--------------------------------2. Realize structure matters, not just size — feedback is deliberately vague (just ratio/size numbers), so the nested-zip-bomb shape has to be reverse-engineered / researched, not read off an error message
--------------------------------3. Build a zero-filled payload.bin (real, genuinely compressible data — not a header lie)
--------------------------------4. Wrap 1 payload.bin per inner_XX.zip (100 total)
--------------------------------5. Group inner zips 10-at-a-time into mid_XX.zip (10 total)
--------------------------------6. Wrap all 10 mid.zip into one outer.zip — this is the file you upload
--------------------------------7. Structure must be exactly 3 nested zip layers, 10-wide fanout at each zip layer
--------------------------------8. Total real uncompressed data must clear 1GB, and ratio (uncompressed / outer.zip file size) must clear 1000:1
--------------------------------9. Upload outer.zip to /upload — server partially decompresses every leaf for real (never trusts declared zip metadata) to confirm it's genuine before granting the flag
-----------------FLAG >> CyC0g{n3st3d_z1p_b0mb_3xp3rt}
-------------(Made By sc0d3r)--------------
